<?xml version="1.0" encoding="utf-8"?>
<!-- views/zvacation_views.xml -->
<odoo>
    <!-- Vista de Lista para las Solicitudes de Vacaciones -->
    <record id="view_zvacation_list" model="ir.ui.view">
        <field name="name">zleave.zvacation.list</field>
        <field name="model">zleave.zvacation</field>
        <field name="arch" type="xml">
            <list string="Solicitudes de Vacaciones">
                <field name="employee_id"/>
                <field name="vacation_type"/>
                <field name="date_from"/>
                <field name="date_to"/>
        
                <field name="state"/>
            </list>
        </field>
    </record>

    <!-- Vista de Formulario para Solicitudes de Vacaciones -->
    <record id="view_zvacation_form" model="ir.ui.view">
        <field name="name">zleave.zvacation.form</field>
        <field name="model">zleave.zvacation</field>
        <field name="arch" type="xml">
            <form string="Vacation Zleave">
                <!-- Cabecera con el Status Bar -->
                <header>
                    <field name="state" widget="statusbar"
                        statusbar_visible="draft,submitted,approved,refused,cancelled"/>

                    <button name="action_send_for_approval"
                            type="object"
                            string="Enviar"
                            class="btn-primary"
                            invisible="state != 'draft'"/>

                    <button name="action_approve"
                            type="object"
                            string="Aprobar"
                            class="btn-primary"
                            invisible="state != 'submitted'"/>

                    <button name="action_refuse"
                            type="object"
                            string="Rechazar"
                            invisible="state != 'submitted'"/>

                    <button name="action_cancel"
                            type="object"
                            string="Anular"
                            invisible="state in ('approved','refused','cancelled')"/>
                    
                </header>

                <sheet>
                    <div>
                        <h1>
                            <field name="name" readonly="1"/>
                        </h1>
                        <h3>
                            <field name="display_name" readonly="1"/>
                        </h3>
                    </div><hr/>

                    <group>
                        <div class="row">
                            <!-- Columna izquierda -->
                            <div class="col-md-6 col-12">
                                <group>
                                    <group string="Datos del Empleado">
                                    <field name="company_id" readonly="1"/>
                                    <field name="employee_id" options="{'no_create': True}" invisible="state != 'draft'"/>
                                    <field name="employee_id" options="{'no_create': True}" readonly="1" invisible="state == 'draft'"/>
                                    <field name="employee_job"/>
                                    </group>                                    
                                </group>

                                <group string ="Detalles de Solicitud">
                                    <field name="requested_by_id" readonly="1"/>
                                    <field name="vacation_type"/>
                                    <field name="date_from" widget="daterange" options="{'end_date_field': 'date_to'}"/>
                                    <field name="date_to" invisible="1"/>
                                    <field name="duration_days" readonly="1"/>
                                </group>

                                <group>
                                    <field name="description" 
                                    placeholder="De manera opcional puede escribir el motivo de sus vacaciones aquí..."/>
                                </group>

                                
                            </div>

                            <!-- Columna derecha -->
                            <div class="col-md-6 col-12 highlight-column"
                                invisible="state != 'draft'">
                                <group>
                                    <h3><i class="fa fa-calendar"></i> Saldo de Vacaciones Actual</h3>
                                </group>

                                <!-- Notebook para mostrar las acumulaciones -->
                                
                                        <!-- Vista de Lista para Mostrar las Acumulaciones -->
                                        <field name="employee_id" invisible="1"/> <!-- Relacionar al empleado actual (hacerlo invisible) -->
                                        <field name="vacation_year_ids" readonly="True" options="{'no_create': True}" >
                                            <list  domain="[('employee_id', '=', employee_id)]">
                                                <field name="year" string="Año"/>
                                                <field name="start_date" string="Fecha Inicial"/>
                                                <field name="end_date" string="Fecha Final"/>                                                
                                                <field name="accumulated_days" string="Días Acumulados"/>
                                                <field name="consumed_days" string="Días Consumidos"/>                                            
                                                <field name="balance_days" string="Saldo"/>
                                            </list>
                                        </field>                                
                                
                                <hr/>
                                <group string="Detalle de Autorizante">
                                    <field name="employee_department_id"/>
                                    <field name="approver_id"/>
                                    <field name="cargo_id"/>
                                </group>
                                    
                            </div>

                            <div class="col-md-6 col-12 highlight-column"
                                invisible="state not in ['submitted','approved']">

                                <group>
                                    <h3><i class="fa fa-calendar-check-o"></i> Asignación aplicada</h3>
                                </group>

                                <field name="allocation_ids" readonly="True" options="{'no_create': True, 'no_edit': True}">
                                    <list>
                                        <field name="vacation_year_id" string="Año"/>
                                        <field name="days_allocated" string="Días asignados"/>
                                        <field name="state" string="Estado"/>
                                    </list>
                                </field>

                                <hr/>
                                <group string="Detalle de Autorizante">
                                    <field name="employee_department_id" readonly="True"/>
                                    <field name="approver_id" readonly="True"/>
                                    <field name="cargo_id" readonly="True"/>
                                </group>
                            </div>


                        </div>
                    </group>                   
                    
                </sheet>

                <chatter>
            
                    <field name="message_follower_ids"/>
                    <field name="message_ids"/>
                    <field name="activity_ids"/>
                </chatter>  
            </form>
        </field>
    </record>

    <!-- Acción para abrir la vista de lista de vacaciones -->
    <record id="action_zvacation" model="ir.actions.act_window">
        <field name="name">Solicitudes de Vacaciones</field>
        <field name="res_model">zleave.zvacation</field>
        <field name="view_mode">list,form</field>
        <field name="target">current</field>
    </record>

    
</odoo>
